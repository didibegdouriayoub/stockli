-- ============================================================================
-- تعيين مشرف جديد — نسخة/لصق في SQL Editor (وليس migration، يُعاد استعماله)
-- ============================================================================
-- الخطوات:
--   1) أنشئ حساباً عادياً بنفس شاشة تسجيل الدخول في التطبيق (رقم هاتف + كلمة
--      مرور خاصة بك أنت، وليس بأي محل حقيقي — اسم المحل هنا غير مهم).
--   2) نفّذ الاستعلام أدناه بعد تعديل رقم الهاتف ليطابق ما استعملته.
-- ============================================================================

insert into public.admins (user_id)
select id from auth.users where email = '0639959512@stockli.app'
on conflict (user_id) do nothing;

-- للتحقق:
-- select u.email, a.created_at from public.admins a join auth.users u on u.id = a.user_id;
